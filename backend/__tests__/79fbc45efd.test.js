// ********RoostGPT********
/*
Test generated by RoostGPT for test NodeMochaTest using AI Type Open AI and AI Model gpt-4-1106-preview


ROOST_TEST_HASH=ad824a76d3

*/

// ********RoostGPT********
const express = require('express');
const path = require('path');
const request = require('supertest');

// Setup express app
const app = express();

app.use(express.static(path.join(__dirname, '../frontend/build')));

app.get('*', (req, res) => {
  res.sendFile(path.join(__dirname, '../frontend/build/index.html'));
});

describe('GET /* - Testing server fallback for SPA', () => {
  
  test('It should serve the index.html file', async () => {
    const response = await request(app).get('/any-route');
    expect(response.status).toBe(200);
    expect(response.type).toBe('text/html');
  });

  test('It should return a 404 status for non-existent files', async () => {
    const response = await request(app).get('/non-existent-file');
    expect(response.status).toBe(404);
  });

  // More test cases can be added here

});

